@model IEnumerable<website_CLB_HTSV.Models.ThamGiaHoatDong>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DaThamGia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DangKyHoatDong)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DaThamGia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DangKyHoatDong.MaDangKy)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaThamGiaHoatDong">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaThamGiaHoatDong">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaThamGiaHoatDong">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
